[pytest]
DJANGO_SETTINGS_MODULE = config.settings

python_files = test_*.py *_tests.py

addopts = --reuse-db --nomigrations -n auto

norecursedirs = .git venv node_modules frontend/dist static media backups
